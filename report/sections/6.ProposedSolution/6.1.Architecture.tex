\section{Architecture Overview}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{overview.png}
    \caption{Streamify architecture overview}
    \label{fig:overview}
\end{figure}

Figure~\ref{fig:overview} shows the end-to-end architecture: Eventsim generates events into a Kafka cluster, Spark Structured Streaming consumes and lands them as Hudi bronze tables, dbt transforms the bronze layer into a star schema and a wide table on the same Spark cluster, and Superset visualizes the result through the Spark Thrift Server, alongside a Prometheus/Grafana monitoring stack.

\begin{table}[H]
    \centering
    \caption{Technology stack per pipeline layer.}
    \label{tab:stack}
    \begin{tabularx}{\textwidth}{|l|l|X|}
        \hline
        \textbf{Layer} & \textbf{Tool} & \textbf{Role} \\
        \hline
        Data simulation & Eventsim & Generates synthetic music-streaming events based on the Million Song Dataset \\
        \hline
        Message queue & Apache Kafka 4.3 (KRaft) & 2-node cluster, replication factor 2, 3 partitions per topic, no ZooKeeper \\
        \hline
        Stream processing & Spark Structured Streaming 3.5.3 & Reads Kafka, decodes/enriches events, writes bronze tables to Hudi \\
        \hline
        Table format / storage & Apache Hudi & MERGE\_ON\_READ bronze streaming tables, COPY\_ON\_WRITE gold/seed tables, on MinIO/S3 via the Hadoop S3A connector \\
        \hline
        Compute cluster & Spark Standalone (1 master, 2 workers) & Shared by the streaming job and dbt (via Thrift), 4 cores total \\
        \hline
        Transformation & dbt (\texttt{dbt-spark}, \texttt{method: thrift}) & SQL-based star schema and wide-table modelling, submitted via the Spark Thrift Server \\
        \hline
        Visualization & Apache Superset & Dashboards connected to the Spark Thrift Server \\
        \hline
        Monitoring & Prometheus, Grafana, cAdvisor & Kafka consumer-group lag, Spark JVM/streaming metrics, per-container CPU/memory \\
        \hline
        Containerization & Docker Compose & Single-command reproducible deployment \\
        \hline
    \end{tabularx}
\end{table}

The remainder of this chapter walks through the pipeline one layer at a time -- ingestion, stream processing, storage, transformation, and serving -- followed by how the whole stack is deployed. Each layer section closes with a short note on why that specific tool was chosen, cross-referencing the fuller alternatives-and-trade-offs discussion in Chapter~\ref{ch:related-work-background}.

\section{Ingestion Layer}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{4/kafka_architecture.png}
    \caption{Kafka internal architecture: 2-node KRaft cluster, 3 partitions per topic, replication factor 2.}
    \label{fig:kafka-architecture}
\end{figure}

Eventsim simulates a real-world user base, generating page-view and song-play events that mimic activity on a music-streaming website. Apache Kafka 4.3 runs in KRaft mode (no ZooKeeper) as a 2-node cluster (\texttt{broker-1}, \texttt{broker-2}). Each of the three event topics -- \texttt{listen\_events}, \texttt{page\_view\_events}, \texttt{auth\_events} -- has 3 partitions and replication factor 2, so every partition has a replica on both brokers, as shown in Figure~\ref{fig:kafka-architecture}.

Because KRaft mode is used, each broker itself also acts as a metadata/controller node, removing the need for a separate ZooKeeper ensemble. For each topic, the 3 partitions are spread across the two brokers, and because replication factor is 2, every partition is fully replicated: one broker holds the leader replica for a given partition and the other holds the follower replica, and the two brokers swap this role across partitions so that neither broker is leader for every partition of a topic. Producers (Eventsim) and consumers (the Spark Structured Streaming job) always read from and write to the leader replica of a partition; the follower replica only pulls data from the leader to stay in sync, and takes over as leader if its broker fails. This is what gives the cluster broker-level fault tolerance despite running on just two nodes.

\textbf{Why Kafka, and why this sizing.} Kafka's durable, partitioned log and per-consumer-group offsets let Structured Streaming resume ingestion exactly where it left off after a restart, which a direct HTTP push or a traditional queue such as RabbitMQ does not provide as directly. The 2-node, replication-factor-2 sizing is the smallest configuration that gives the cluster any broker-level fault tolerance at all, chosen over a single broker (no fault tolerance) or a larger cluster (more resource cost than this project's single-machine deployment justifies).

\section{Stream Processing Layer}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{4/spark_architecture.png}
    \caption{Spark cluster architecture: driver, master, and worker/executor processes.}
    \label{fig:spark-architecture}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{4/spark_batch_streaming_processing.png}
    \caption{Spark Structured Streaming's micro-batch model: an unbounded stream is sliced into small batches, each run as an ordinary Spark batch job.}
    \label{fig:spark-micro-batch}
\end{figure}

A long-running Spark Structured Streaming job consumes all three Kafka topics, fixes latin1-mojibake on text fields, derives time columns, and appends each micro-batch to bronze Hudi tables (\texttt{staging.<topic>}). Figure~\ref{fig:spark-architecture} shows the cluster this job runs on, and Figure~\ref{fig:spark-micro-batch} shows how Structured Streaming turns an unbounded Kafka stream into the sequence of discrete jobs Spark actually executes.

The streaming job runs as a driver program submitted to the Spark Standalone cluster described in Section~\ref{sec:proposed-cluster-setup} (1 master, 2 workers, 4 cores total, shared with the Thrift Server dbt submits through). Structured Streaming does not process events one at a time; instead, the driver repeatedly polls the three Kafka topics for new offsets, groups the newly available records into a micro-batch, and schedules that micro-batch as a short-lived Spark job whose tasks run on the worker executors, in the same way a batch job would. Each micro-batch's tasks read their assigned Kafka partitions, apply the transformations from Listing~\ref{lst:build-stream}, and append the results to the bronze Hudi tables before the driver commits the checkpoint and moves on to the next batch. Because this job and the Thrift Server share the same 4-core cluster, the size and frequency of micro-batches are bounded by whatever cores are not already claimed by the Thrift Server at that moment.

Listing~\ref{lst:build-stream} shows the core of the job that reads a Kafka topic, decodes it, and derives the time columns later used to partition the bronze Hudi tables.

\begin{lstlisting}[language=Python, caption={Building a Hudi bronze stream from a Kafka topic (excerpt from \texttt{spark\_streaming/jobs/stream\_events.py}).}, label={lst:build-stream}]
def build_stream(spark, topic, decode_strings):
    raw = (
        spark.readStream.format("kafka")
        .option("kafka.bootstrap.servers", KAFKA_BROKERS)
        .option("subscribe", topic)
        .option("startingOffsets", "earliest")
        .load()
    )

    parsed = raw.select(
        F.from_json(F.col("value").cast("string"), SCHEMAS[topic]).alias("data"),
        "partition",
        "offset",
    ).select("data.*", "partition", "offset")

    if decode_strings:
        parsed = (
            parsed.withColumn("artist", decode_udf(F.col("artist")))
                  .withColumn("song", decode_udf(F.col("song")))
        )

    ts_col = F.timestamp_millis(F.col("ts"))
    return (
        parsed
        .withColumn("ts_timestamp", ts_col)
        .withColumn("year_", F.year(ts_col))
        .withColumn("month_", F.month(ts_col))
        .withColumn("day_", F.dayofmonth(ts_col))
        .withColumn("hour_", F.hour(ts_col))
        .withColumn("processed_time", F.current_timestamp())
        .withColumn("_row_key", F.expr("uuid()"))
    )
\end{lstlisting}

Structured Streaming resumes from a persisted checkpoint volume (\texttt{spark-checkpoints}) on restart; each micro-batch also commits its Kafka offsets to a \texttt{streamify-<topic>} consumer group purely so lag is visible in Grafana (Spark itself never depends on that commit to resume).

\textbf{Why Spark Structured Streaming, and not Flink.} An earlier version of this project used Apache Flink for this job. It was replaced with Spark Structured Streaming so that stream processing runs on the same engine and cluster that dbt already needs for batch transformation (avoiding operating two separate distributed runtimes on a 4-core cluster), and to reuse Hudi's first-class Spark data source support. The trade-off accepted is latency: Flink's event-at-a-time model can achieve lower per-event latency than Spark's micro-batch model.

\section{Storage Layer}

Tables are Hudi-managed, backed by MinIO/S3 locally or real S3 in \texttt{aws} mode via the Hadoop S3A connector, with no rebuild needed to switch between the two -- only an environment variable (\texttt{LAKE\_ENV}). There is no separate catalog service: each table is self-describing at its storage path (a \texttt{.hoodie/} metadata directory) and is (re-)registered into Spark's catalog idempotently at job startup. Bronze tables use the MERGE\_ON\_READ table type, so fast streaming appends land as delta log files that are periodically compacted into columnar base files; gold and seed tables use COPY\_ON\_WRITE, rebuilt via full overwrite.

\textbf{Why Hudi, and why MOR bronze / COW gold.} Hudi's self-describing \texttt{.hoodie/} metadata avoids the extra catalog or warehouse service that plain Parquet or Snowflake would require. Bronze uses MOR to keep frequent streaming appends cheap; gold uses COW because it is rebuilt once per run and then read repeatedly by Superset.

\section{Transformation Layer}
\label{sec:transformation-layer}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{star_schema.png}
    \caption{Star schema: \texttt{fact\_streams} and its five dimension tables.}
    \label{fig:star-schema}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{dbt_graph.png}
    \caption{dbt DAG showing the dependency between bronze sources, the star schema, and \texttt{wide\_streams}.}
    \label{fig:dbt-graph}
\end{figure}

dbt~\cite{dbt} connects to the Spark cluster via the Spark Thrift Server (\texttt{method: thrift}) and builds two model layers directly on top of the bronze Hudi tables, materialized as COPY\_ON\_WRITE Hudi tables rebuilt via full overwrite each run. Figure~\ref{fig:star-schema} shows the resulting star schema, and Figure~\ref{fig:dbt-graph} shows the dbt DAG that builds it:

\begin{itemize}
    \item \textbf{Star schema.} One fact table, \texttt{fact\_streams}, joined to five dimension tables: \texttt{dim\_users}, \texttt{dim\_songs}, \texttt{dim\_artists}, \texttt{dim\_locations}, and \texttt{dim\_datetime}. This is the normalized, storage-efficient model (Section~\ref{sec:core-theory}).
    \item \textbf{Wide table (\texttt{wide\_streams}).} A denormalized table built from the star schema by pre-joining \texttt{fact\_streams} with all five dimension tables, so that Superset can query a single flat table instead of performing joins at dashboard-query time.
\end{itemize}

\textbf{Why dbt, and why both a star schema and a wide table.} dbt was chosen over a layer of hand-written Spark SQL scripts because it declares model dependencies explicitly (via \texttt{ref()}/\texttt{source()}), runs a built-in test framework against every model (uniqueness, not-null, referential integrity), and generates documentation from the same source files -- properties that matter given the five-dimension join the fact table depends on. Modelling both a star schema and a separate wide table, rather than either alone, keeps normalization and storage efficiency in the star schema while keeping BI query simplicity in \texttt{wide\_streams}, instead of trading one off against the other.

\section{Serving Layer}
\label{sec:serving-layer}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{dashboard.png}
    \caption{Superset results dashboard, built on top of \texttt{wide\_streams} and queried through the Spark Thrift Server.}
    \label{fig:results-dashboard}
\end{figure}

Apache Superset~\cite{superset} connects to the same Spark Thrift Server that dbt submits SQL through, querying the \texttt{wide\_streams} model to visualize insights through interactive dashboards (\texttt{superset/provision\_dashboard.py} provisions them via the REST API). Figure~\ref{fig:results-dashboard} shows the resulting dashboard layout.

Prometheus scrapes Kafka consumer-group lag (via kafka-exporter), Spark's built-in Prometheus metrics, and per-container stats (via cAdvisor); Grafana visualizes all of it across dashboards.

\textbf{Why Superset.} Superset ships a native Thrift/HiveServer2-compatible connector that talks directly to the Spark Thrift Server endpoint already used by dbt, needing no extra license or gateway infrastructure, unlike Power BI or Tableau.

\section{Deployment \& Environment}
\label{sec:proposed-cluster-setup}

The whole stack is deployed with Docker Compose (\texttt{docker-compose.yml}), with a \texttt{docker-compose.dev.yml} overlay that adds MinIO for local, S3-free development. Spark runs as a Standalone cluster of 1 master and 2 workers, 2 cores / 2 GB each (4 cores total), shared between the streaming ingestion job and the Spark Thrift Server that dbt submits SQL through, each capped at 2 cores.

\textbf{Why Spark Standalone at this size.} Spark Standalone was chosen over YARN or Kubernetes because both alternatives add a separate resource-management control plane that is not justified when the entire compute budget is 4 cores across one master and two workers; Standalone instead runs as three containers alongside the rest of the pipeline in the same \texttt{docker-compose.yml}. The direct consequence is that the streaming job and the Thrift Server, each capped at a fixed 2 cores, together already consume the entire cluster, leaving no headroom for ad-hoc Spark SQL while both are running.


\section{Monitoring as Qualitative Evidence}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{grafana_kafka.png}
    \caption{Grafana Kafka Lag dashboard: consumer-group lag and consumption rate per topic.}
    \label{fig:grafana-kafka}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{grafana_spark.png}
    \caption{Grafana Spark dashboard: streaming/batch job metrics, JVM heap, and per-container CPU/memory.}
    \label{fig:grafana-spark}
\end{figure}

The Grafana dashboards backed by Prometheus, shown in Figures~\ref{fig:grafana-kafka} and~\ref{fig:grafana-spark}, provide qualitative evidence of the pipeline's throughput and health under load, rather than a formal benchmark.
