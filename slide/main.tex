\documentclass[aspectratio=169]{beamer}
\usetheme{Madrid}
\usefonttheme{structurebold}
\usefonttheme[onlymath]{serif}

\definecolor{BKTemplate}{rgb}{0.11765, 0.30588, 0.47451}
\usecolortheme[named=BKTemplate]{structure}
\setbeamercolor{title}{fg=BKTemplate,bg=}
\setbeamertemplate{caption}[default]   % no "Figure X:" prefix
\setbeamertemplate{navigation symbols}{}

% ── No footer bar; page number as plain overlay bottom-right ──────────────────
\setbeamertemplate{footline}{%
  \hfill%
  \usebeamerfont{page number in head/foot}%
  \insertframenumber\,/\,\inserttotalframenumber%
  \hspace{1.5ex}\vspace{1ex}%
}

\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{microtype}
\usepackage{multicol}
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta}
\usepackage{amsmath}
\usepackage{tabularx}

\graphicspath{{images/}}

\renewcommand{\footnotesize}{\tiny}

%------------------------------------------------------------
\title[Streaming Processing in BigData Platform]
{\vspace*{0.5cm}\\\textbf{VIETTEL DIGITAL TALENT 2026}\\\textbf{Streaming Processing in BigData Platform}}

\subtitle{Real-time data pipelines with Apache Kafka \& Apache Flink}

\author[Ngo Quang Hieu]{
  Mentor: Nguyen Minh Hieu \\
  Student: Ngo Quang Hieu
}

\institute[VDT 2026]{
  Data Engineer Track \\
  Viettel Digital Talent 2026
}

\date[July 2026]{July 1$^{st}$, 2026}
%------------------------------------------------------------

\begin{document}

% ── Frame 1: Title ────────────────────────────────────────────────────────────
\setbeamertemplate{background canvas}{
  \includegraphics[width=\paperwidth,height=\paperheight]{images/FrontBackgroundvtt.png}
}
\begin{frame}[plain]
\titlepage
\end{frame}

% ── Background for all remaining slides ───────────────────────────────────────
\setbeamertemplate{background canvas}{
  \includegraphics[width=\paperwidth,height=\paperheight]{images/Backgroundvtt.png}
}

% ── Section progress slide at the start of each section ───────────────────────
\AtBeginSection[]{
  \begin{frame}{Table of Contents}
    \tableofcontents[currentsection, sectionstyle=show/shaded, subsectionstyle=show/show/shaded]
  \end{frame}
}

% ── Frame 2: Table of Contents ────────────────────────────────────────────────
\begin{frame}{Table of Contents}
\tableofcontents
\end{frame}

%=============================================================================
\section{Introduction}
%=============================================================================

% ── Frame 3: Why Stream Processing? ──────────────────────────────────────────
\begin{frame}{Why Stream Processing?}
\begin{columns}
  \column{0.50\textwidth}
  \textbf{The Problem}
  \begin{itemize}
    \item \textbf{High latency} — jobs run hourly/daily, so insights lag reality by hours
    \item \textbf{Stale decisions} — by the time data is processed, the moment to act has passed
    \item \textbf{Resource spikes} — large periodic loads strain infra, then sit idle
    % \item \textbf{Costly reprocessing} — a failure mid-job means rerunning the entire batch, not just what's new
  \end{itemize}

  \begin{center}
  \begin{tikzpicture}
    \node[draw, fill=red!10, rounded corners, minimum width=3.2cm, minimum height=0.6cm, align=center, font=\small] (b) {Batch: hours delay};
    \node[draw, fill=green!15, rounded corners, minimum width=3.2cm, minimum height=0.6cm, align=center, font=\small, below=0.5cm of b] (s) {Stream: milliseconds};
    \draw[-{Stealth}, thick, BKTemplate] (b) -- (s);
  \end{tikzpicture}
  \end{center}

  \column{0.47\textwidth}
  \textbf{Stream Processing Enables}
  \begin{itemize}
    % \item \textbf{Real-time personalisation} — recommendations/pricing react within ms of user action
    \item \textbf{Live monitoring \& alerting} — detect anomalies or failures as they happen, not next morning
    \item \textbf{Continuous aggregation} — dashboards show current counts/metrics, not a stale snapshot
    \item \textbf{Smooth, steady load} — events processed as they arrive, avoiding the resource spikes of periodic batch runs
  \end{itemize}

\end{columns}
\end{frame}

% ── Frame 4: Objectives & Scope ───────────────────────────────────────────────
\begin{frame}{Objectives}
  \textbf{Objectives}
  \begin{enumerate}\setlength\itemsep{3pt}
    \item Overview \textbf{Apache Kafka} as message queue
    \item Compare \textbf{Spark Streaming vs Flink} for streaming processing
    \item Build ELT streaming pipeline
    \item Demonstrate real-time dashboard with live metrics
  \end{enumerate}

\end{frame}

%=============================================================================
\section{Message Queue: Apache Kafka}
%=============================================================================

% ── Frame 5: Why Kafka? ───────────────────────────────────────────────────────
\begin{frame}{Why Apache Kafka?}
\begin{columns}
  \column{0.48\textwidth}
  \textbf{Problems Without a Message Queue}
  \begin{itemize}
    \item \textbf{Tight coupling} — producer knows every consumer
    \item \textbf{No buffering} — slow consumer → data loss
    \item \textbf{No replay} — consumed = gone forever
    \item \textbf{Fan-out cost} — $N$ consumers = $N$ calls
  \end{itemize}

  \column{0.48\textwidth}
  \textbf{How Kafka Solves It}
  \begin{itemize}
    \item \textbf{Decoupling} — write to topic, subscribe independently
    \item \textbf{Durable buffer} — persisted to disk, replicated
    \item \textbf{Replay} — configurable retention; re-read anytime
    \item \textbf{Scalability} — partitions scale throughput linearly
    \item \textbf{Fan-out free} — multiple groups, own offsets
  \end{itemize}
\end{columns}
\end{frame}

% ── Frame 6: Kafka Architecture ───────────────────────────────────────────────
\begin{frame}{Kafka Architecture}
\begin{columns}
  \column{0.52\textwidth}
  \includegraphics[width=\textwidth]{kafka/kafka_architecture.png}

  \column{0.45\textwidth}
  \begin{itemize}
    \item \textbf{Producer} — partitions by key; batches \& compresses; \texttt{acks=all} for durability
    \item \textbf{Broker} — leader/follower replicas; append-only log; sequential I/O
    \item \textbf{Topic} — immutable records; time-based or log-compaction retention
    \item \textbf{Partition} — ordered, offset-indexed; unit of parallelism
    \item \textbf{Consumer Group} — one partition → one member; independent offset per group
  \end{itemize}
  \vspace{0.2cm}
  \small\textit{Max parallelism = number of partitions}
\end{columns}
\end{frame}

%=============================================================================
\section{Streaming Processing Engines}
%=============================================================================

% ── Frame 7: Batch vs Stream ──────────────────────────────────────────────────
\begin{frame}{Batch vs.\ Stream Processing}
\begin{columns}
  \column{0.48\textwidth}
  \begin{block}{Batch Processing}
    \begin{itemize}
      \item Collect → store → process in bulk
      \item Latency: \textbf{hours to days}
      \item Tools: Hadoop, Spark, scheduled SQL
    \end{itemize}
  \end{block}
  \vspace{0.3cm}
  \begin{block}{Stream Processing}
    \begin{itemize}
      \item Compute \emph{continuously} as records arrive
      \item Latency: \textbf{milliseconds to seconds}
      \item Tools: Apache Flink, Spark Streaming
    \end{itemize}
  \end{block}

  \column{0.48\textwidth}
  \textbf{Key Challenges in Streaming}
  \begin{enumerate}
    \item \textbf{Unbounded data} — stream never ends; state must be bounded
    \item \textbf{Out-of-order events} — need watermarks to handle late arrivals
    \item \textbf{Latency} — must process quickly to keep up with incoming data
    \item \textbf{Fault tolerance} — recover without loss or duplicates; exactly-once semantics
    \item \textbf{State \& Checkpoint Management} — large state must be checkpointed to durable storage (e.g., local, cloud)
  \end{enumerate}
  \end{columns}
\end{frame}

% ── Frame 8b: DAG-based Dataflow ─────────────────────────────────────────────
\begin{frame}{DAG-based Dataflow: Shared Foundation}
\begin{columns}
  \column{0.55\textwidth}
  \centering
  \includegraphics[width=\textwidth]{engine/dataflow.png}
  \par\vspace{2pt}{\tiny Both Spark and Flink represent computation as a Directed Acyclic Graph (DAG)}

  \column{0.42\textwidth}
  \textbf{What is a Dataflow DAG?}
  \begin{itemize}\small
    \item \textbf{Directed} — data flows one way; no cycles
    \item \textbf{Acyclic} — guarantees deterministic, terminating execution
    \item \textbf{Graph} — \textbf{nodes} are operators (map, filter, join, aggregate); \textbf{edges} are data streams between them
    \item Each operator processes records and passes results downstream
    \item Operators at the same level run \textbf{in parallel} across partitions
    \item Both Spark \& Flink compile your code into a DAG — they differ in \emph{when} and \emph{how} they execute it
  \end{itemize}
\end{columns}
\end{frame}

% ── Frame 9: Apache Flink ────────────────────────────────────────────────────
\begin{frame}{Apache Flink: True Event Streaming}
\begin{columns}
  \column{0.55\textwidth}
  \includegraphics[width=\textwidth]{engine/flink_dataflow_unbounded_stream.png}
  \par\vspace{1pt}{\tiny True streaming: records flow through operator DAG one-by-one}
  \vspace{0.15cm}

  \includegraphics[width=\textwidth]{engine/flink_checkpoint.png}
  \par\vspace{1pt}{\tiny Chandy-Lamport barriers: async snapshots without pausing processing}

  \column{0.42\textwidth}
  \textbf{Dataflow Model}
  \begin{itemize}\small
    \item \textbf{Record-at-a-time} — no batching delay
    \item Operators form a DAG; data flows continuously
    \item \textbf{Event time + watermarks} — correct out-of-order handling
  \end{itemize}
  \vspace{0.25cm}
  \textbf{Fault Tolerance}
  \begin{itemize}\small
    \item Barrier checkpoints injected into streams like a conveyor belt
    \item \textbf{Exactly-once} with transactional sinks
    \item State management can be store in heap or RockDB
    \item Checkpoint is fully async, no pause in processing (local or cloud)
  \end{itemize}
\end{columns}
\end{frame}


% ── Frame 8: Apache Spark ────────────────────────────────────────────────────
\begin{frame}{Apache Spark Structured Streaming}
\begin{columns}
  \column{0.55\textwidth}
  \includegraphics[width=\textwidth]{engine/spark_micro_batch_latency.png}
  % \par\vspace{1pt}{\tiny Micro-batch: stream as a sequence of small DataFrames}
  % \vspace{0.15cm}

  \includegraphics[width=\textwidth]{engine/spark_rtm.png}
  % \par\vspace{1pt}{\tiny Real-time Mode (Spark 4): concurrent stages, streaming shuffle}

  \column{0.42\textwidth}
  \textbf{Micro-batch (default)}
  \begin{itemize}\small
    \item Latency: \textbf{100 ms – seconds}
    \item Still depend on exist batch mode stage, plan create a lot of latency
    \item State management In-memory+WAL or RocksDB
    \item Checkpoint sync write to checkpoint location (local or cloud) create latency
  \end{itemize}
  \vspace{0.25cm}
  \textbf{Real-time Mode (Spark 4)}
  \begin{itemize}\small
    \item Inherit from micro-batch, but update \textbf{concurrent stages} and \textbf{longer duration micro-batches}
    \item Mitigate the latency of micro-batch in planning phase and sequential stages
    \item Latency: \textbf{sub-100 ms}
  \end{itemize}
  \vspace{0.2cm}
\end{columns}
\end{frame}


% ── Frame 10: Spark vs Flink ─────────────────────────────────────────────────
\begin{frame}{Spark vs.\ Flink: Side-by-Side}
\centering
\begin{tabularx}{\textwidth}{lXX}
  \toprule
  \textbf{Dimension}  & \textbf{Apache Spark}                            & \textbf{Apache Flink} \\
  \midrule
  Execution model     & Micro-batch (scheduled triggers); Real-time Mode (Spark 4) & True record-at-a-time streaming \\
  Latency             & 100 ms–s (micro-batch); sub-100 ms (RTM)         & 1–10 ms \\
  State management    & In-memory / RocksDB (Spark 3.2+); managed per trigger & RocksDB or heap; always-live, keyed state \\
  Checkpoint          & WAL per micro-batch                              & Async Chandy-Lamport barriers; incremental snapshots \\
  Use case            & Unified batch + streaming                        & Low-latency, stateful, CEP, event-time joins \\
  \bottomrule
\end{tabularx}
\vspace{0.3cm}
\end{frame}

%=============================================================================
\section{Application: Streamify Pipeline}
%=============================================================================

% ── Frame 11: Why Streamify? ─────────────────────────────────────────────────
\begin{frame}{Why Streamify?}
\begin{columns}
  \column{0.50\textwidth}
  \textbf{The Use Case}
  \begin{itemize}\small
    \item Music streaming platforms generate \textbf{continuous, high-velocity events} — every play, skip, pause, and login is an event
    \item Users expect \textbf{live charts} and real-time fraud detection — batch pipelines cannot deliver this
    \item A realistic domain to validate all theoretical concepts end-to-end
  \end{itemize}
  \vspace{0.15cm}
  \textbf{3 Event Types Simulated}
  \begin{itemize}\small
    \item \texttt{listen\_events} — song plays, skips, durations
    \item \texttt{page\_view\_events} — user navigation behaviour
    \item \texttt{auth\_events} — logins, session starts/ends
  \end{itemize}

  \column{0.47\textwidth}
  \textbf{What Streamify Validates}
  \begin{itemize}\small
    \item \textbf{Kafka} — durable, decoupled event bus between producer and all downstream consumers
    \item \textbf{PyFlink} — low-latency, exactly-once ingestion into Snowflake
    \item \textbf{dbt} — transforms raw staging data into an analytics-ready star schema
    \item \textbf{Airflow} — orchestrates transformation runs reliably
    \item \textbf{Power BI} — surfaces live insights from a pre-joined wide view
  \end{itemize}
\end{columns}
\end{frame}

% ── Frame 12: Architecture Overview ──────────────────────────────────────────
\begin{frame}{Streamify: End-to-End Architecture}
\centering
\includegraphics[width=0.80\textwidth]{app/overview.png}
\end{frame}

% ── Frame 12: Pipeline + Modeling ────────────────────────────────────────────
\begin{frame}{Pipeline: Ingest → Process → Model → Serve}
\begin{columns}
  \column{0.48\textwidth}
  \textbf{Real-time Ingestion}
  \begin{itemize}\small
    \item \textbf{Eventsim} — generates 3 Kafka topics:\\
          \texttt{listen\_events}, \texttt{page\_view\_events}, \texttt{auth\_events}
    \item \textbf{Kafka} — durable, partitioned transport
    \item \textbf{PyFlink} — parses JSON, enriches timestamps,\\
          writes to Snowflake via JDBC (exactly-once)
  \end{itemize}
  \vspace{0.3cm}
  \textbf{Transformation \& Orchestration}
  \begin{itemize}\small
    \item \textbf{dbt} — builds star schema inside Snowflake\\
          (fact\_streams + 5 dim tables + wide\_streams view)
    \item \textbf{Airflow} — schedules dbt run via DockerOperator
  \end{itemize}

  \column{0.48\textwidth}
  \centering
  \begin{tikzpicture}[node distance=0.55cm, every node/.style={font=\small}]
    \node[draw, fill=BKTemplate!15, rounded corners, minimum width=3.2cm, align=center] (ev) {Eventsim};
    \node[draw, fill=BKTemplate!25, rounded corners, minimum width=3.2cm, align=center, below=of ev] (kf) {Apache Kafka};
    \node[draw, fill=BKTemplate!35, rounded corners, minimum width=3.2cm, align=center, below=of kf] (fl) {Apache Flink (PyFlink)};
    \node[draw, fill=BKTemplate!45, rounded corners, minimum width=3.2cm, align=center, below=of fl] (sf) {Snowflake (STAGING)};
    \node[draw, fill=BKTemplate!55, rounded corners, minimum width=3.2cm, align=center, below=of sf] (dbt) {dbt (transformation)};
    \node[draw, fill=BKTemplate!65, rounded corners, minimum width=3.2cm, align=center, below=of dbt] (bi) {Dashboard};
    \draw[-{Stealth}] (ev) -- (kf);
    \draw[-{Stealth}] (kf) -- (fl);
    \draw[-{Stealth}] (fl) -- (sf);
    \draw[-{Stealth}] (sf) -- (dbt);
    \draw[-{Stealth}] (dbt) -- (bi);
  \end{tikzpicture}
\end{columns}
\end{frame}

% ── Frame 13: Dashboard ───────────────────────────────────────────────────────
\begin{frame}{Dashboard: Real-time Insights}
\begin{columns}
  \column{0.65\textwidth}
  \includegraphics[width=\textwidth]{app/dashboard.png}

  \column{0.33\textwidth}
  \textbf{Key Metrics}
  \begin{itemize}
    \item Top artists by play count
    \item Songs played over time (hourly)
    \item Geographic distribution (US map)
    \item Free vs.\ paid listener split
  \end{itemize}
\end{columns}
\end{frame}

%=============================================================================
\section{Conclusion}
%=============================================================================

% ── Frame 14: Conclusion ──────────────────────────────────────────────────────
\begin{frame}{Conclusion}
\textbf{Key Takeaways}
\begin{enumerate}
  \item \textbf{Kafka} decouples producers from consumers — swap the processing engine without touching the rest of the pipeline
  \item \textbf{Flink} delivers sub-10 ms latency, native event-time semantics, and exactly-once guarantees — outperforming Spark for stateful real-time workloads
  \item \textbf{ELT pattern} keeps concerns separate: Flink loads raw events, dbt transforms inside Snowflake, Airflow orchestrates
\end{enumerate}

\textbf{Future Directions}
\begin{itemize}
  \item \textbf{Cloud} — migrate to Kinesis + Confluent Cloud
  \item \textbf{Schema evolution} — Confluent Schema Registry + Avro
\end{itemize}

\end{frame}

% ── Frame 15: The End ─────────────────────────────────────────────────────────
\setbeamertemplate{footline}{}
\begin{frame}
  \centering
  \vspace{1.2cm}
  \textbf{\Huge{- THE END -}}\\
  \vspace{0.5cm}
  \textit{\Large{Thank you for your attention}}\\
  \vspace{0.5cm}
  \textbf{Contact:}\\
  Ngo Quang Hieu --- Viettel Digital Talent 2026
\end{frame}

\end{document}
